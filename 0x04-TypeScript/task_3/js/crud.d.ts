import { RowID, RowElement } from "./interface";

export const insertRow = (row: RowElement): number => {};
export const deleteRow = (rowId: RowID): void => {};
export const updateRow = (rowId: RowID, row: RowElement): RowID => {};


